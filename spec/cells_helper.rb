require '/classes/grid.rb'

RSpec.describe Grid, "#countOfcells" do
  context "cuenta el numero de celulas vivas y muertas" do
    it "regresa la matriz modificada" do
      dibujo = Grid.new
      dibujo.dibujar(grid, 10, 10)
      expect()
    end
  end
end